# Application Layer

Esta camada contém os casos de uso da aplicação financeira.

## Usecases
Classes que implementam a lógica de aplicação, orquestrando entidades e repositórios (ex: AddLancamentoUseCase, GetContasUseCase).